
int	ft_strcmp(char *s1, char *s2)
{
	/* compare *s1 et *s2
	 * if s1 = s2 return 0
	 * if s1 < s2 return -
	 * if s1 > s2 return +
	 * when it says that it compare it compare not the length but the ascii
	 * value of each character in the string*/

	int	as1;
	int	counts1;
	int	as2;
	int	counts2;
	int	i;
	
	while(s1[i])
	{

		

}
